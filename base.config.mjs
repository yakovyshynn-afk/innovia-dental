// Єдине джерело правди для базового шляху деплою — раніше значення було задубльоване
// (`base` у vite.config.js і хардкод `BASE` у scripts/fix-asset-base.mjs), що при переїзді
// на власний домен ламалося мовчки: хтось міняв тільки vite.config.js, скрипт лишався зі
// старим префіксом і дописував неіснуючий шлях до /images/... без жодної помилки збірки.
// Тепер обидва місця імпортують значення звідси. GitHub Pages цього проєкту — підкаталог
// (/innovia-dental/), власний домен у майбутньому — просто змінити це значення на '/'.
export const BASE = '/innovia-dental/'
